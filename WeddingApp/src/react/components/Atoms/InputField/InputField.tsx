import React from 'react';
// import Fields from './variants';
import { IRegField } from '../../../../Types/RegField';


export const InputField: React.StatelessComponent<IRegField> = ({type, name, title, value, className, placeHolder}) => {
    return null;
    //  React.createElement(Fields[type], {name, title, value, className, placeHolder, type});
}